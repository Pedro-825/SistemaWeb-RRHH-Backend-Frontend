# URL base del backend. Vacío para usar rutas relativas (proxy Vite/nginx).
VITE_API_URL=http://localhost:8080

# Activar mocks de datos (true/false). Por defecto desactivado.
VITE_ENABLE_MOCKS=false
